{
  "name": "@pyng/cloudflare-relay",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@pyng/shared": "file:../shared"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260517.1",
    "wrangler": "^4.92.0"
  }
}
